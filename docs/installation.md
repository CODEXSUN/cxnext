# For data fetching/caching from Laravel API
```
pnpm add @tanstack/react-query
```
# For form handling (e.g., user registration, orders)
```
pnpm add react-hook-form  
```
# Accessible UI components
```
pnpm add @headlessui/react @heroicons/react  
```

# Icons for ERP navigation
```
pnpm add lucide-react     
```

# Optional: For auth with Laravel Sanctum
# Dev tool, so save as dev dep
```
pnpm add -D @tanstack/react-query-devtools  
```

pnpm approve-builds


npx shadcn@latest add button input label navigation-menu sheet card separator